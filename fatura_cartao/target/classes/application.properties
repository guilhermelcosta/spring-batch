# Configura��o do banco de dados prim�rio, para armazenar meta-dados
spring.datasource.jdbcUrl=jdbc:mysql://localhost:3306/spring_batch
spring.datasource.username=user
spring.datasource.password=root

# Configura��o do banco de dados secund�rio, para armazenar os dados da aplica��o
app.datasource.jdbcUrl=jdbc:mysql://localhost:3306/fatura_cartao_credito
app.datasource.username=user
app.datasource.password=root

# Configura��es gerais
spring.batch.jdbc.initialize-schema=always
# Setar para always caso seja necess�rio rodar o data.sql
spring.sql.init.mode=never
